<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pmd.ubi.pt.surrealistwriter">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity
            android:name=".CreateAcc"
            android:label="@string/title_registration_activity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".OfflineMode" />
        <activity
            android:name=".Ranking"
            android:label="Ranking" />
        <activity android:name=".OfflineGameSettings" />
        <activity android:name=".OfflineGamePlay" />
        <activity android:name=".OfflinePlayerName" />
        <activity android:name=".OnlineMode" />
        <activity android:name=".CreateRoom" />
        <activity android:name=".CurrentRoom" />
        <activity android:name=".OfflineGameEnd"></activity>
    </application>

</manifest>